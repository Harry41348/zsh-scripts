#!/usr/bin/env zsh
# This script finds a project directory, moves into it, and opens it in VS Code.
# Usage: ./proj.sh [project-name] [directory-type]
# Get project name from arguments
# Find project directory in freelance folder, or projects folder